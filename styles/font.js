import {Chakra_Petch, Inter, Roboto, Tomorrow} from '@next/font/google';

export const header = Chakra_Petch ({
  weight: '600',
  subsets: ['latin'],
});

export const navbar = Tomorrow ({
  weight: '400',
  subsets: ['latin'],
});

export const content = Inter ({
  weight: '400',
  subsets: ['latin'],
});

export const button = Roboto ({
  weight: '400',
  subsets: ['latin'],
});
